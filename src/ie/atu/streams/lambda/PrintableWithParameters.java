package ie.atu.streams.lambda;

public interface PrintableWithParameters {
    // Simple abstract method that takes no arguments and returns a string
    public void print(String prefix, String suffix);
}
